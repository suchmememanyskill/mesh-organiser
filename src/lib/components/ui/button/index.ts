import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from "./button.svelte";

import AsyncButton from "./async-button.svelte";

export {
	Root,
	type ButtonProps as Props,
	//
	Root as Button,
	AsyncButton,
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
};
